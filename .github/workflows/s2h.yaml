name: mritd/s2h

on:
  workflow_call:
  workflow_dispatch:
    inputs:
      trigger:
        description: Manually trigger
        required: true
        type: choice
        options:
          - build

jobs:
  s2h:
    runs-on: ubuntu-latest
    secrets: inherit
    steps:
      - uses: ./.github/workflows/.earthly.yaml
        with:
          build-dir: s2h

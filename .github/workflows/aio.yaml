name: All In One

on:
  schedule:
    - cron: '0 8 * * 1'
  workflow_dispatch:
    inputs:
      trigger:
        description: 'Manually trigger'
        required: true
        type: choice
        options:
          - all
          - alpine
          - ssh
          - test

env:
  FORCE_COLOR: 1
  DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
  DOCKERHUB_PASSWORD: ${{ secrets.DOCKERHUB_PASSWORD }}

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "${{ toJson(jobs) }}"
      - run: echo "${{ toJson(job) }}"
  alpine:
    runs-on: ubuntu-latest
    concurrency: alpine
    if: contains(github.event.inputs.trigger, 'xxxxxxxxx', 'alpine-base', 'all') || github.event_name == 'schedule'
    steps:
    - name: Init Earthly
      uses: mritd/init-earthly-action@main
    - name: Earthly Build
      working-directory: xxxxxxxxx
      run: earthly --allow-privileged --push +all
    
  ssh:
    needs: alpine
    runs-on: ubuntu-latest
    concurrency: alpine
    if: contains(github.event.inputs.trigger, 'xxxxxxxxx', 'alpine-base', 'all') || github.event_name == 'schedule'
    steps:
    - name: Init Earthly
      uses: mritd/init-earthly-action@main
    - name: Earthly Build
      working-directory: xxxxxxxxx
      run: earthly --allow-privileged --push +all

  s2h:
    needs: alpine
    runs-on: ubuntu-latest
    concurrency: alpine
    if: contains(github.event.inputs.trigger, 'xxxxxxxxx', 'alpine-base', 'all') || github.event_name == 'schedule'
    steps:
    - name: Init Earthly
      uses: mritd/init-earthly-action@main
    - name: Earthly Build
      working-directory: xxxxxxxxx
      run: earthly --allow-privileged --push +all

  makeself:
    needs: alpine
    runs-on: ubuntu-latest
    concurrency: alpine
    if: contains(github.event.inputs.trigger, 'xxxxxxxxx', 'alpine-base', 'all') || github.event_name == 'schedule'
    steps:
    - name: Init Earthly
      uses: mritd/init-earthly-action@main
    - name: Earthly Build
      working-directory: xxxxxxxxx
      run: earthly --allow-privileged --push +all

  derper:
    needs: alpine
    runs-on: ubuntu-latest
    concurrency: alpine
    if: contains(github.event.inputs.trigger, 'xxxxxxxxx', 'alpine-base', 'all') || github.event_name == 'schedule'
    steps:
    - name: Init Earthly
      uses: mritd/init-earthly-action@main
    - name: Earthly Build
      working-directory: xxxxxxxxx
      run: earthly --allow-privileged --push +all

  container-cli:
    needs: alpine
    runs-on: ubuntu-latest
    concurrency: alpine
    if: contains(github.event.inputs.trigger, 'xxxxxxxxx', 'alpine-base', 'all') || github.event_name == 'schedule'
    steps:
    - name: Init Earthly
      uses: mritd/init-earthly-action@main
    - name: Earthly Build
      working-directory: xxxxxxxxx
      run: earthly --allow-privileged --push +all

  aria2:
    needs: alpine
    runs-on: ubuntu-latest
    concurrency: alpine
    if: contains(github.event.inputs.trigger, 'xxxxxxxxx', 'alpine-base', 'all') || github.event_name == 'schedule'
    steps:
    - name: Init Earthly
      uses: mritd/init-earthly-action@main
    - name: Earthly Build
      working-directory: xxxxxxxxx
      run: earthly --allow-privileged --push +all

  bandwagonmon:
    needs: alpine
    runs-on: ubuntu-latest
    concurrency: alpine
    if: contains(github.event.inputs.trigger, 'xxxxxxxxx', 'alpine-base', 'all') || github.event_name == 'schedule'
    steps:
    - name: Init Earthly
      uses: mritd/init-earthly-action@main
    - name: Earthly Build
      working-directory: xxxxxxxxx
      run: earthly --allow-privileged --push +all

  ddns:
    needs: alpine
    runs-on: ubuntu-latest
    concurrency: alpine
    if: contains(github.event.inputs.trigger, 'xxxxxxxxx', 'alpine-base', 'all') || github.event_name == 'schedule'
    steps:
    - name: Init Earthly
      uses: mritd/init-earthly-action@main
    - name: Earthly Build
      working-directory: xxxxxxxxx
      run: earthly --allow-privileged --push +all

  dnsacme:
    needs: alpine
    runs-on: ubuntu-latest
    concurrency: alpine
    if: contains(github.event.inputs.trigger, 'xxxxxxxxx', 'alpine-base', 'all') || github.event_name == 'schedule'
    steps:
    - name: Init Earthly
      uses: mritd/init-earthly-action@main
    - name: Earthly Build
      working-directory: xxxxxxxxx
      run: earthly --allow-privileged --push +all

  idgen:
    needs: alpine
    runs-on: ubuntu-latest
    concurrency: alpine
    if: contains(github.event.inputs.trigger, 'xxxxxxxxx', 'alpine-base', 'all') || github.event_name == 'schedule'
    steps:
    - name: Init Earthly
      uses: mritd/init-earthly-action@main
    - name: Earthly Build
      working-directory: xxxxxxxxx
      run: earthly --allow-privileged --push +all

  notibot:
    needs: alpine
    runs-on: ubuntu-latest
    concurrency: alpine
    if: contains(github.event.inputs.trigger, 'xxxxxxxxx', 'alpine-base', 'all') || github.event_name == 'schedule'
    steps:
    - name: Init Earthly
      uses: mritd/init-earthly-action@main
    - name: Earthly Build
      working-directory: xxxxxxxxx
      run: earthly --allow-privileged --push +all

  clash-meta:
    needs: alpine
    runs-on: ubuntu-latest
    concurrency: alpine
    if: contains(github.event.inputs.trigger, 'xxxxxxxxx', 'alpine-base', 'all') || github.event_name == 'schedule'
    steps:
    - name: Init Earthly
      uses: mritd/init-earthly-action@main
    - name: Earthly Build
      working-directory: xxxxxxxxx
      run: earthly --allow-privileged --push +all

##########################################################################

  caddy:
    runs-on: ubuntu-latest
    concurrency: buildkit
    if: github.event.inputs.trigger == 'caddy' || github.event.inputs.trigger == 'all' || github.event_name == 'schedule'
    steps:
    - name: Init Earthly
      uses: mritd/init-earthly-action@main
    - name: Earthly Build
      working-directory: xxxxxxxxx
      run: earthly --allow-privileged --push +all

  nodebuild:
    runs-on: ubuntu-latest
    concurrency: buildkit
    if: github.event.inputs.trigger == 'nodebuild' || github.event.inputs.trigger == 'all' || github.event_name == 'schedule'
    steps:
    - name: Init Earthly
      uses: mritd/init-earthly-action@main
    - name: Earthly Build
      working-directory: xxxxxxxxx
      run: earthly --allow-privileged --push +all

  openjdk:
    runs-on: ubuntu-latest
    concurrency: buildkit
    if: github.event.inputs.trigger == 'openjdk' || github.event.inputs.trigger == 'all' || github.event_name == 'schedule'
    steps:
    - name: Init Earthly
      uses: mritd/init-earthly-action@main
    - name: Earthly Build
      working-directory: xxxxxxxxx
      run: earthly --allow-privileged --push +all

  open-vm-tools:
    runs-on: ubuntu-latest
    concurrency: buildkit
    if: github.event.inputs.trigger == 'open-vm-tools' || github.event.inputs.trigger == 'all' || github.event_name == 'schedule'
    steps:
    - name: Init Earthly
      uses: mritd/init-earthly-action@main
    - name: Earthly Build
      working-directory: xxxxxxxxx
      run: earthly --allow-privileged --push +all

  nginx-singlepage:
    runs-on: ubuntu-latest
    concurrency: buildkit
    if: github.event.inputs.trigger == 'nginx-singlepage' || github.event.inputs.trigger == 'all' || github.event_name == 'schedule'
    steps:
    - name: Init Earthly
      uses: mritd/init-earthly-action@main
    - name: Earthly Build
      working-directory: xxxxxxxxx
      run: earthly --allow-privileged --push +all


name: mritd/idgen

on:
  workflow_call:
  workflow_dispatch:
    inputs:
      trigger:
        description: Manually trigger
        required: true
        type: choice
        options:
          - build

jobs:
  idgen:
    runs-on: ubuntu-latest
    steps:
      - uses: ./.github/workflows/.earthly.yaml
        with:
          build-dir: idgen

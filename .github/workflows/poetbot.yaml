name: mritd/poetbot

on:
  workflow_call:
  workflow_dispatch:
    inputs:
      trigger:
        description: Manually trigger
        required: true
        type: choice
        options:
          - build

jobs:
  poetbot:
    runs-on: ubuntu-latest
    uses: ./.github/workflows/.earthly.yaml
    secrets: inherit
    with:
      build-dir: poetbot
